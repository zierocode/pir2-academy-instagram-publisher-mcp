# Security Policy

Report vulnerabilities privately to the repository owner. Do not include Instagram access tokens, authorization codes, signed Canva export URLs, captions, or learner account data in an issue.

The publisher accepts only HTTPS image URLs, stores credentials in the operating-system credential vault, and requires an explicit preview confirmation before the public write.
