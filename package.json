{
  "name": "pir2-academy-instagram-publisher",
  "version": "0.1.1",
  "description": "MCP แบบ local สำหรับ preview และโพสต์ภาพไป Instagram โดย PiR2 Academy",
  "license": "MIT",
  "type": "module",
  "main": "./server/index.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bundle": "node scripts/pack.mjs",
    "bundle:verify": "node scripts/verify-bundle.mjs",
    "bundle:test": "vitest run tests/integration/bundle.test.ts",
    "check": "npm run typecheck && npm test",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "smoke": "vitest run tests/smoke/startup.test.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.tests.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "@napi-rs/keyring": "1.3.0",
    "open": "11.0.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "22.18.6",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
